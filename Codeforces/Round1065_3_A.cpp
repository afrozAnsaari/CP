#include <bits/stdc++.h>

using namespace std;
int main()
{

    ios::sync_with_stdio(0);
    cin.tie(nullptr);

    int t;
    cint >> t;

    while (t--)
    {
        int n;
        cin >> n;
        if (n & 1)
        {
            cout << 0 << '\n';
        }
        else
        {
            
        }
    }
    return 0;
}