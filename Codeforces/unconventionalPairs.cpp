#include <iostream>

using namespace std;

int main(int argc, char const *argv[])
{

    int nTest;
    cin >> nTest;

    for (int i = 0; i < nTest; i++)
    {
        int n;
        cin >> n;
        int a[n];
        int odd = 1, even = 0;
        for (int j = 0; j < n; j++)
        {
            cin >> a[j];
        }

        for (int j = 0; j < n / 2; j++)
        {
            
        }
    }

    return 0;
}
