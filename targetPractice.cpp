#include <bits/stdc++.h>
using namespace std;

int main()
{

    ios::sync_with_stdio(0);
    cin.tie(nullptr);

    vector<string> ss(10);
    for (int i = 0; i < 10; i++)
        cin >> ss[i];

    int score = 1;
    int ans = 0;

    for (int i = 0; i < 10; i++)
    {
        for (int j = 0; j < 10; j++)
        {
            if (c == '#')
            {
                ans += score *
            }
        }
    }
}